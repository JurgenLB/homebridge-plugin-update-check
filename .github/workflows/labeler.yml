name: Labeler

on: [pull_request]

jobs:
  labeler:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: homebridge/.github/.github/workflows/labeler.yml@latest
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}